; ─── PHP Runtime ─────────────────────────────────────────────────────────────
[PHP]
post_max_size = 420M
upload_max_filesize = 400M
expose_php = Off
realpath_cache_size = 32M
realpath_cache_ttl = 720
memory_limit = 256M
max_execution_time = 60
max_input_time = 5
register_argc_argv = 0
date.timezone = ${TZ:-UTC}

; Security
display_errors = Off
log_errors = On

; Sessions
session.gc_maxlifetime = 7200
session.cookie_lifetime = 0

; ─── APCu (Laravel config/routes cache) ──────────────────────────────────────
apc.enabled = 1
apc.enable_cli = 1

; ─── OPCache (essential for Octane) ──────────────────────────────────────────
[Opcache]
opcache.enable = 1
opcache.enable_cli = 1
opcache.memory_consumption = 256
opcache.use_cwd = 0
opcache.save_comments = 1           ; required for Filament attributes
opcache.max_file_size = 0
opcache.max_accelerated_files = 32531
opcache.validate_timestamps = 0
opcache.file_update_protection = 0
opcache.interned_strings_buffer = 32
opcache.enable_file_override = 1
opcache.file_cache_consistency_checks = 0
opcache.file_cache = /tmp/opcache-file-cache

; ─── JIT (PHP 8.5 tracing JIT) ──────────────────────────────────────────────
[JIT]
opcache.jit_buffer_size = 128M
opcache.jit = tracing
opcache.jit_hot_loop = 16
opcache.jit_hot_func = 32
opcache.jit_hot_return = 4
opcache.jit_max_root_traces = 2048
opcache.jit_max_side_traces = 256
